<%= link_to "Back to all lists", lists_path %>
<hr>
<div class="container">
  <%= simple_form_for(@list) do |f| %>
    <%= f.input :name, :placeholder => 'How do you want to call your new list?'%>
    <%= f.button :submit, class: "btn-primary" %>
  <% end %>
</div>
